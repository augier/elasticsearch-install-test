box: wercker/php
build:
  steps:

    - augier/elasticsearch-install:
        version: 1.3.1

    - script:
        name: Listen to ports
        code: sudo lsof -i TCP | grep 9200

    - script:
        name: Test elasticsearch
        code: curl -I 'http://192.168.0.1:9200/_status'