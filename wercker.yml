box: wercker/php
services:
  - wunki/elasticsearch@0.0.2
build:
  steps:

    - augier/elasticsearch-install:
        version: 1.3.1

    - script:
        name: Check status
        code: sudo service elasticsearch status